apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: ecom-1
  region: us-west-2

managedNodeGroups:
  - name: managed-ng-public
    instanceType: t2.medium
    desiredCapacity: 4
    spot: true