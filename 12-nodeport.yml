apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
  labels:
    purpose: nodeport-demon
spec:
  type: NodePort
  selector:
    purpose: nodeport-demon
    project: ecom
    environment: Dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
    
---
apiVersion: v1
kind: Pod
metadata:
  name: nginix
  labels:
    purpose: nodeport-demon
    project: ecom
    environment: Dev
spec:
  containers:
    - name: mysecret-container
      image: nginx
